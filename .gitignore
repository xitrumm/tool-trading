# Telethon session — BÍ MẬT: ai có file này là đăng nhập được tài khoản Telegram
*.session
*.session-journal

# Cấu hình chứa API_ID/API_HASH thật — BÍ MẬT
config.txt

# Danh sách token bot đích — BÍ MẬT (token điều khiển được bot)
target_bots.txt

# Database & file tạm của bot
trading_memory.db
temp_data.xlsx

# Log cmd ghi ra file — có thể chứa nội dung tin/kèo nhạy cảm
logs/

# Python
__pycache__/
*.pyc
.venv/
venv/
